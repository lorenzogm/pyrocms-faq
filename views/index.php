<h1>Preguntas frecuentes</h1>

<div id="faq">
	{{ categories:entries }}
	<div class="row category">
		<p class="title"><a href="{{ url:site }}faq/{{ slug }}">{{ name }}</a></p>
		<p class="description">{{ description }}</p>
		<a href="{{ url:site }}faq/{{ slug }}" class="btn btn-sm btn-primary">Leer más</a>
	</div>
	{{ /categories:entries }}
</div>
